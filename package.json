{
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev-jacket": "vercel link --project=bh-briet-jacket --scope=brickhousecoop --yes && vercel pull && vercel dev",
    "dev-tagger": "vercel link --project=bh-briet-tagger --scope=brickhousecoop --yes && vercel pull && vercel dev",
    "dev-server": "vercel link --project=bh-briet-server --scope=brickhousecoop --yes && vercel pull && vercel dev",
    "dev-market": "vercel link --project=bh-briet-market --scope=brickhousecoop --yes && vercel pull && vercel dev",
    "dev-reader": "vercel link --project=bh-briet-reader --scope=brickhousecoop --yes && vercel pull && vercel dev",
    "dev": "dotenv -e .vercel/.env -c development -- turbo run dev --no-cache --parallel --continue",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "build:jacket": "dotenv -e .vercel/.env -c development -- turbo run build --filter=bh-briet-jacket",
    "build:tagger": "dotenv -e .vercel/.env -c development -- turbo run build --filter=bh-briet-tagger",
    "build:server": "dotenv -e .vercel/.env -c development -- turbo run build --filter=bh-briet-server",
    "build:market": "dotenv -e .vercel/.env -c development -- turbo run build --filter=bh-briet-market",
    "build:reader": "dotenv -e .vercel/.env -c development -- turbo run build --filter=bh-briet-reader"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-turbo": "^1.12.4",
    "turbo": "^2.1.1",
    "vercel": "^56.3.2"
  },
  "license": "UNLICENSED",
  "private": true,
  "packageManager": "npm@10.8.1",
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "24.x",
      "onFail": "warn"
    },
    "packageManager": {
      "name": "npm",
      "version": ">=10",
      "onFail": "warn"
    }
  },
  "name": "bh-briet"
}
