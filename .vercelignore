# Excluded from `vercel deploy` uploads (CLI deploys from the repo root upload
# the whole tree; git-integration deploys ignore this file).
backups
eli
.turbo

# Env files carry real credentials; never ship them in an upload.
**/.env*
