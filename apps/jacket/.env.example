# Copy to .env.local and fill in the token (ask another developer, or pull from
# Vercel with `vercel env pull`).

# Sanity CMS. The project ID is public; the token is not, and is required —
# both datasets are private, so without it reads return no documents.
NEXT_PUBLIC_SANITY_PROJECTID=3lm68n5v
NEXT_PUBLIC_SANITY_DATASET=development
SANITY_TOKEN=

# Fathom analytics (optional, no-op if unset)
NEXT_PUBLIC_FATHOM_SITEID=
