# Loaded by `sanity dev`. These are not secrets (the project ID is public);
# your access is controlled by `npx sanity login`.
#
# Tagger points at `production` on purpose — it is the CMS, so editors need the
# real catalog. The apps that read the catalog default to `development` instead.
# Edits made here are live.
SANITY_STUDIO_PROJECTID=3lm68n5v
SANITY_STUDIO_DATASET=production
